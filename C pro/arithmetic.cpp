//wap in c use of all arithmatico ope...
#include<stdio.h>
int main() {
    int a,b,add,sub,mul,div,rem;
    printf("enter two no\n");
    scanf("%d %d",&a,&b);
    add=a+b;
    sub=a-b;
    mul=a*b;
    div=a/b;
    rem=a%b;
    printf("Add of two no = %d",add);
    printf("\nsub of two no = %d",sub);
    printf("\nmul of two no = %d",mul);
    printf("\ndiv of two no = %d",div);
    printf("\nrem of two no = %d",rem);


    
    return 0;
}